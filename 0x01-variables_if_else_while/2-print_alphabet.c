#include <stdio.h>
/**
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	/* Print characters 'a' to 'z' */
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
